import UIKit

// sets are key values with fixed size
// can modify the value but keeping the same type

let fullName = (firstName: "Aldair", lastName: "Zamora")

// can access values with index or key
fullName.0
fullName.firstName
